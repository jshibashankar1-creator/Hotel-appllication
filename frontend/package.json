{
  "name": "hotelhub-web-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build && node -e \"import('fs').then(fs => { fs.copyFileSync('public/_redirects', 'dist/_redirects'); fs.mkdirSync('frontend/dist', {recursive: true}); fs.cpSync('dist', 'frontend/dist', {recursive: true}); })\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide": "^0.395.0"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
